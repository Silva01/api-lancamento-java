spring:
  datasource:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        validator:
          apply_to_ddl: true
        check_nullability: true
        dialect: org.hibernate.dialect.H2Dialect